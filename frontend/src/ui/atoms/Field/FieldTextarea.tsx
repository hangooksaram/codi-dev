import theme from "@/ui/theme";
import styled from "@emotion/styled";
import { Field } from "formik";
import { textareaStyle } from "../Input/Textarea";

const FieldTextarea = styled(Field)(textareaStyle);

export default FieldTextarea;

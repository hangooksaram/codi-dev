import MentorCenterClientLayout from './clientLayout';

export default function MentorCenterLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <MentorCenterClientLayout>{children}</MentorCenterClientLayout>;
}

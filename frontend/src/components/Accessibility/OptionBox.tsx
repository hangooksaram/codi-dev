import styled from '@emotion/styled';

const OptionBox = styled.div(() => ({
  padding: '24px 0px',
  borderTop: '1px solid #858e9b',
  borderBottom: '1px solid #858e9b',
}));

export default OptionBox;
